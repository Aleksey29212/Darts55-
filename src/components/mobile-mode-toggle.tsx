'use client';

/**
 * Component removed to avoid duplication.
 * All mode switching logic is now integrated into the compact Header switcher.
 */
export function MobileModeToggle() {
    return null;
}
